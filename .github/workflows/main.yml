name: review
on: [push]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Check
        env:
          GITHUB_TOKEN: ${{ github.token }}
        uses: simbo/vale-action@v1
