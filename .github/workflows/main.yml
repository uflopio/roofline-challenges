name: review
on: [push]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Spellcheck
        uses: simbo/vale-action@v1
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          fail_on_error: false
