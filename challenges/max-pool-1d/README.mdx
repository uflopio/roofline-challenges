---
slug: max-pool-1d
title: 1D Max Pooling
difficulty: EASY
ranked: false
tags: [pooling]
supporterOnly: true
---

Perform 1D max pooling on an input tensor:
$$
\text{output}[i] = \max_{m=0}^{k-1} \text{input}[S \cdot i + D \cdot m - P]
$$

The max pooling operation slides a window of size $k \times k$ over the input tensor with stride $S$, dilation $D$, and padding $P$, computing the maximum value within each window position.

## Input
- Matrix `input` of size $H$ (input tensor)
- `kernel_size` ($k$): Size of the pooling window
- `stride` ($S$): Step size between window positions
- `padding` ($P$): Number of zero-padding elements added on all sides
- `dilation` ($D$): Spacing between kernel elements

## Output
- Matrix `output` of size $H_{\text{out}}$ where:
  $$H_{\text{out}} = \left\lfloor\frac{H + 2P - D(k-1) - 1}{S}\right\rfloor + 1$$

## Sizes

- $KERNEL_SIZE = 2^{21}, STRIDE = 7, PADDING = 4, DILATION = 3, H = 1$
- $KERNEL_SIZE = 2^{22}, STRIDE = 2, PADDING = 1, DILATION = 0, H = 1$
- $KERNEL_SIZE = 2^{23}, STRIDE = 3, PADDING = 2, DILATION = 1, H = 1$
- $KERNEL_SIZE = 2^{24}, STRIDE = 4, PADDING = 2, DILATION = 1, H = 2$
- $KERNEL_SIZE = 2^{25}, STRIDE = 3, PADDING = 1, DILATION = 1, H = 1$
- $KERNEL_SIZE = 2^{26}, STRIDE = 5, PADDING = 3, DILATION = 2, H = 1$
