#include <cuda_runtime.h>

// Compute s = sum(a[0..n-1]). `s` is a single-element output buffer.
// a, s are device pointers; you allocate kernels + pick launch config.
void solve(const float *a, float *s, int n) {

}
