#include <cuda_runtime.h>

void solve(const float* input_image, int height, int width, int kernel_size, float* output_image) {
  // Solution goes here.
  // Keep in mind: the pointers are on the GPU!
}
