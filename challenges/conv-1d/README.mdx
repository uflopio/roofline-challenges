---
slug: conv-1d
title: 1D Convolution
difficulty: EASY
ranked: false
tags: [convolution]
---

Perform 1D convolution between an input signal and a kernel with zero padding and centered kernel.

Let $r = \frac{K-1}{2}$. Out-of-bounds accesses to $A$ are treated as zero.

$$
C[i] = \sum_{j=0}^{K-1} A[\,i + j - r\,] \cdot B[j]
$$

The convolution operation slides the kernel over the input signal, computing the sum of element-wise multiplications at each position. Zero padding is used at the boundaries.

## Input
- Vector $A$ of size $N$ (input signal)
- Vector $B$ of size $K$ (convolution kernel)

## Output
- Vector $C$ of size $N$ (convolved signal)

## Sizes

- $N = 2^{16}, K = 8191$
- $N = 2^{15}, K = 8191$
- $N = 2^{17}, K = 8191$
- $N = 2^{19}, K = 8191$
