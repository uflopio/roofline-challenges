#include <cuda_runtime.h>

// Implement vector addition: c[i] = a[i] + b[i] for i in 0..n-1.
// a, b, c are device pointers; you allocate kernels + pick launch config.
void solve(const float *a, const float *b, float *c, int n) {

}
